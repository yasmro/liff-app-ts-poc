import React from 'react'
import { Link } from 'react-router-dom'

const VoucherList = ():React.ReactElement => {
    return (
        <div>
            VoucherList
            <Link to="/">Top</Link>
        </div>
    )
}

export default VoucherList
